{
    "rules": {
        "users": {
            "$uid": {
                ".read": "$uid === auth.uid",
                ".write": "$uid === auth.uid"
            }
        },
        "rooms": {
            ".read": "auth !== null",
            ".write": "auth !== null"
        },
        "leagues": {
            "$code": {
                ".read": "auth !== null",
                ".write": "auth !== null",
                "players": {
                    "$uid": {
                        ".write": "$uid === auth.uid || auth !== null"
                    }
                }
            }
        },
        "communityCategories": {
            ".read": true,
            ".write": "auth !== null",
            "$catId": {
                "upvotedBy": {
                    "$uid": {
                        ".write": "$uid === auth.uid"
                    }
                }
            }
        }
    }
}
